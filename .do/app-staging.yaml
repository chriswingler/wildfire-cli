name: wildfire-discord-bot-staging # Changed name
region: nyc
services:
- name: wildfire-bot-staging # Changed service name
  github:
    repo: chriswingler/wildfire-cli # This should be the dynamic repo name later
    branch: main
  run_command: cd /app && python src/main.py
  environment_slug: python
  instance_count: 1
  instance_size_slug: basic-xxs # Staging might use a smaller instance
  source_dir: /
  http_port: 8080
# Potentially add environment variables specific to staging here later
# envs:
# - key: APP_ENV
#   value: staging
#   type: GENERAL
